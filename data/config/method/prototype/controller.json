{
    "_note": "Controller behaviour: probe sampling, effector params, ServiceProfile dispatch overrides, InputProfile shape. Empty placeholder; populated at stage 7 when prototype/controller/ lands."
}
