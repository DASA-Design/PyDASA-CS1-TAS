{
    "_note": "TAS workflow graph spec: pickTask -> {vitalParamsMsg -> analyseData -> {changeDrug | changeDose | sendAlarm}; buttonMsg -> triggerAlarm}. Empty placeholder; populated at stage 6 when prototype/target/workflow/loader.py lands."
}
