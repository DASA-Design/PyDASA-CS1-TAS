{
  "method": "stochastic",
  "description": "SimPy DES parameters for the stochastic method.",
  "seed": 42,
  "horizon_invocations": 10000,
  "warmup_invocations": 1000,
  "replications": 10,
  "confidence_level": 0.95,
  "window_invocations": 1000,
  "rsem_target": 0.05,
  "notes": "10 000 invocations per replication with 1 000-invocation warmup cut (Welch), 10 replications for 95 % CIs. Matches [13] Section V-B extended-sample-size setup; RSEM=5% accuracy setting."
}
