# Ignore every file under data/results/ (all JSONs are regenerated by
# `python -m src.methods.<method>` runs — never hand-edit).
# The directory structure itself stays tracked via .gitkeep files.
*

# Exceptions: keep the directory markers and this rules file in git
!*/
!.gitkeep
!.gitignore
